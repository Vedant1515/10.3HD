apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-blue
  namespace: staging
  labels:
    app: backend
    color: blue
spec:
  selector:
    matchLabels:
      app: backend
      color: blue
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
    scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
    - staging
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-green
  namespace: staging
  labels:
    app: backend
    color: green
spec:
  selector:
    matchLabels:
      app: backend
      color: green
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
    scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
    - staging